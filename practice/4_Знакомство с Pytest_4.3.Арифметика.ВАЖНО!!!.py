'''
Важные нюансы
Всегда используйте префикс math. при вызове функций (если импортировали через import math).
Тригонометрические функции работают с радианами, а не градусами. Используйте math.radians() для конвертации.
Некоторые функции могут возвращать math.nan или вызывать ошибки при некорректных входных данных (например, math.sqrt(-1)).
Модуль math работает с числами с плавающей точкой (float), даже если входные данные — целые числа.
'''

########################
#Как тестировать чтобы збежать проблем
#import math - import math — команда, которая подключает встроенный модуль math к вашему скрипту. После этого вы получаете доступ к математическим функциям и константам, которых нет в базовом Python.
#Способ 1. Используйте math.isclose()
#Проверяет, достаточно ли близки два числа (с учётом погрешности):

import math

assert math.isclose(0.1 + 0.1 + 0.1, 0.3) #True
assert math.isclose(result, expected, rel_tol=1e-9) # Относительная погрешность 1e-9
#abs_tol — абсолютная погрешность (например, 0,01 для денег).


########################
#Способ 2. Округление перед сравнением
#Если знаете, сколько знаков после запятой нужно:
assert round(0.1 + 0.1 + 0.1, 10) == round(0.3, 10) #число 10 — это количество знаков после запятой, до которого округляется число.
#Минус: может скрыть реальные ошибки, если округлять слишком грубо.


#######################
#Способ 3. Используйте модуль decimal для точных расчётов
#Подходит для финансов, где важна точность:
from decimal import Decimal

a = Decimal('0.1')
b = Decimal('0.2')
assert a + b == Decimal('0.3') #True


#Пример:
from decimal import Decimal

price1 = Decimal('19.99')  # цена товара 1
price2 = Decimal('5.50')   # цена товара 2
total = price1 + price2

print(f"Общая сумма: {total} руб.")  # Общая сумма: 25.49 руб.

# Проверка равенства (работает точно!)
assert total == Decimal('25.49')  # не вызовет ошибки
print("Расчёт верен!")
#Важно: передавайте числа в виде строк ('0.1'), а не чисел (0.1), иначе ошибка возникнет ещё на этапе создания Decimal.


'''print(plum('0.0123...', '-1.0917...'))
 Результат: -1.07944979999999992238... (очень точный)

print(0.0123... - 1.0917...)
 Результат: -1.0794498 (округлённый)
'''



########################
#Способ 4. Используйте модуль fractions для рациональных чисел
#Если работаете с дробями:
from fractions import Fraction #fractions — встроенный модуль Python для работы с рациональными числами (дробями).Fraction — класс из этого модуля, который представляет дробь в виде числителя и знаменателя.

assert Fraction(1, 10) + Fraction(2, 10) == Fraction(3, 10) #True
#Используйте Fraction, когда нужна точная арифметика с дробями (финансовые расчёты, математические алгоритмы и т. д.).


########################
#Способ 5. Форматируйте вывод для пользователя
#Чтобы не пугать пользователя длинными дробями:
print(f'Итого: {total: 2f} руб') #Выведет 'Итого: 90.90 руб'


########################
#Способ 6. Используйте math.fsum() для точного суммирования
#Уменьшает накопление ошибок при сложении многих чисел:
import math

value = [0.1] * 10
assert math.fsum(value) == 1.0 # True (в отличае от sum (value))

'''
Краткий итог для тестировщика
Помните: 0.1 + 0.1 + 0.1 != 0.3 из‑за особенностей хранения чисел в компьютере.
Не используйте == для сравнения чисел с плавающей запятой.
Используйте:
math.isclose() — для большинства тестов;
decimal.Decimal — для финансовых расчётов;
округление (round) — если знаете требуемую точность;
форматирование строк — для вывода пользователю.
Проверяйте:
граничные случаи (малые/большие числа);
накопление ошибок (много операций подряд);
отображение в интерфейсе.
Уточняйте требования: какая точность нужна в проекте (2 знака после запятой для денег, 6 знаков для научных расчётов и т. д.).
'''



















